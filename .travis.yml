language: node_js
node_js:
    - "8"
cache:
    directories:
        - "node_modules"
sudo: enabled
os: linux
script:
- ./build.sh

deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    on:
        branch: gh-pages
